// #include <bits/stdc++.h> is just a shortcut header that includes almost every standard C++ library in one line.

/* <iostream>
<vector>
<algorithm>
<map>
<set>
<string>
<cmath> */

// USER INPUT-OUTPUT:

#include <iostream>
using namespace std;

int main()
{
    /* int x, y;

    cout << "Enter the values of x: ";
    cin >> x;
    cout << "Enter the value of y: ";
    cin >> y;

    cout << "Value of x is " << x << " and y is " << y; */

// DATA TYPES:

    /* int
    int x = 5;

    long
    long x = 15;

    long long x = 130000000000;

    float and double
    float x = 2.3;
    float y = 6;
    cout << "value of y: " << y; */

    /* string s1, s2;
    cout << "Enter a string: " << endl;
    cin >> s1 >> s2;

    cout << "Name: " << s1 << " " << s2;

    // OR

    // getline takes the entire line and prints it.
    string name1, name2;
    getline(cin, name1);
    getline(cin, name2);
    cout << "Name: " << name1;
    cout << "Name: " << name2;

    int n;
    cin >> n;
    //cin.ignore(); // Refer Notes
    string s;
    getline(cin, s);
    cout << "Message: " << s; */

    /* char ch = 'a';
    cout << (int)ch;
    cout << ch; */

// IF-ELSE:

    /* int marks;
    cout << "Enter your marks: ";
    cin >> marks;

    if(marks < 0 || marks > 100) {
        cout << "Invalid marks";
    }
    else if(marks >= 90) {
        cout << "Grade A";
    }
    else if(marks >= 80) {
        cout << "Grade B";
    }
    else if(marks >= 70) {
        cout << "Grade C";
    }
    else if(marks >= 60) {
        cout << "Grade D";
    }
    else if(marks >= 50) {
        cout << "Grade E";
    }
    else {
        cout << "Fail";
    } */

// SWITCH STATEMENT

    /* int day;
    cin >> day;

    switch (expression)
    {
    case constant expression:
        // code
        break;
    
    default:
        break;
    } */

    return 0;
}